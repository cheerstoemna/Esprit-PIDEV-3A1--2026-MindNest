<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="controllers.TherapyLayoutController"
            fx:id="rootPane"
            stylesheets="@../css/therapy.css"
            styleClass="root">

    <top>
        <HBox spacing="10" styleClass="topbar" BorderPane.alignment="CENTER">
            <Label text="MindNest • Therapy Module" styleClass="title"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="userLabel" styleClass="subtitle"/>
            <Button fx:id="btnTheme" text="🌙 Dark" onAction="#toggleTheme" styleClass="theme-toggle-btn"/>
        </HBox>
    </top>

    <left>
        <VBox spacing="10" styleClass="sidebar" BorderPane.alignment="CENTER">
            <Button fx:id="btnClient" text="Client" onAction="#openClient" styleClass="nav-btn"/>
            <Button fx:id="btnAdmin"  text="Admin"  onAction="#openAdmin"  styleClass="nav-btn"/>
            <Region VBox.vgrow="ALWAYS"/>
            <Label fx:id="themeLabel" text="Theme: Calm Light" styleClass="hint"/>
        </VBox>
    </left>

    <center>
        <StackPane fx:id="contentHost" style="-fx-padding: 18;"/>
    </center>

</BorderPane>